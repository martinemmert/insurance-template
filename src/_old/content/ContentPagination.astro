---

---

<nav class="pagination">
  <a href="#" class="pagination__link">
    <span class="pagination__label">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"></polyline></svg>
      Vorheriger Artikel
    </span>
    <span class="pagination__title">Private Krankenversicherung: Lohnt sich der Wechsel?</span>
  </a>
  <a href="#" class="pagination__link pagination__link--next">
    <span class="pagination__label">
      Nächster Artikel
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"></polyline></svg>
    </span>
    <span class="pagination__title">Altersvorsorge: Die besten Strategien für jeden Lebensabschnitt</span>
  </a>
</nav>

<style>
  /* Pagination */
  .pagination {
    margin: 3rem 0;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }
  .pagination__link {
    padding: 1.5rem;
    background: var(--color-bg-warm);
    border-radius: var(--radius-md);
    text-decoration: none;
    color: inherit;
    transition: background var(--transition-fast);
  }
  .pagination__link:hover { background: var(--color-bg-warm-alt); }
  .pagination__link--next { text-align: right; }
  .pagination__label {
    font-size: var(--text-xs);
    color: var(--color-text-muted);
    margin-bottom: 0.375rem;
    display: flex;
    align-items: center;
    gap: 0.375rem;
  }
  .pagination__link--next .pagination__label {
    justify-content: flex-end;
  }
  .pagination__title {
    font-weight: var(--font-semibold);
  }

  @media (max-width: 768px) {
    .pagination { grid-template-columns: 1fr; }
  }
</style>
