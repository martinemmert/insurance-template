---

---

<!-- Q&A Item -->
<div class="qa-item">
  <div class="qa-question">
    <span class="qa-label">F</span>
    <p>Kann ich die Beiträge von der Steuer absetzen?</p>
  </div>
  <div class="qa-answer">
    <span class="qa-label">A</span>
    <p>Ja, die Beiträge zur BU-Versicherung können als Vorsorgeaufwendungen in der Steuererklärung geltend gemacht werden. Der Höchstbetrag für Arbeitnehmer liegt bei 1.900 Euro pro Jahr (für Selbstständige bei 2.800 Euro). Allerdings konkurrieren die BU-Beiträge mit anderen Versicherungen wie der Krankenversicherung um diesen Höchstbetrag.</p>
  </div>
</div>

<style>
  /* Q&A Format */
  .qa-item {
    margin: 2rem 1.5rem;
    padding: 1.75rem 2rem;
    background: var(--color-bg-warm);
    border-radius: var(--radius-md);
  }
  .qa-question {
    display: flex;
    gap: 1rem;
    margin-bottom: 1rem;
  }
  .qa-label {
    flex-shrink: 0;
    width: 2rem;
    height: 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: var(--font-bold);
    font-size: var(--text-sm);
    border-radius: 0.375rem;
  }
  .qa-question .qa-label {
    background: var(--color-primary);
    color: var(--color-white);
  }
  .qa-question p {
    font-weight: var(--font-semibold);
    margin: 0;
  }
  .qa-answer {
    display: flex;
    gap: 1rem;
  }
  .qa-answer .qa-label {
    background: var(--color-primary-bg);
    color: var(--color-primary);
  }
  .qa-answer p {
    margin: 0;
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
  }
</style>
