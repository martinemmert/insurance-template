---
---

<header class="article-header">
  <nav class="breadcrumb">
    <a href="#">Startseite</a>
    <span class="breadcrumb__separator">/</span>
    <a href="#">Ratgeber</a>
    <span class="breadcrumb__separator">/</span>
    <span>Berufsunfähigkeit</span>
  </nav>
  <span class="article-header__category">Berufsunfähigkeit</span>
  <h1 class="article-header__title">Berufsunfähigkeitsversicherung: Der komplette Ratgeber für 2024</h1>
  <p class="article-header__subtitle">Alles, was Sie wissen müssen, um die richtige BU-Versicherung zu finden – von den Grundlagen bis zu den wichtigsten Klauseln.</p>
  <div class="article-header__meta">
    <div class="article-header__author">
      <img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=100&h=100&fit=crop" alt="Helmut Emmert" class="article-header__author-image">
      <div>
        <div class="article-header__author-name">Helmut Emmert</div>
      </div>
    </div>
    <div class="article-header__date">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect><line x1="16" y1="2" x2="16" y2="6"></line><line x1="8" y1="2" x2="8" y2="6"></line><line x1="3" y1="10" x2="21" y2="10"></line></svg>
      15. Januar 2024
    </div>
    <div class="article-header__reading-time">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"></circle><polyline points="12 6 12 12 16 14"></polyline></svg>
      12 Min. Lesezeit
    </div>
  </div>
</header>

<style>
  .article-header {
    padding: 3rem 1.5rem 2rem;
  }
  .breadcrumb {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    margin-bottom: 1.5rem;
  }
  .breadcrumb a {
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color var(--transition-fast);
  }
  .breadcrumb a:hover { color: var(--color-primary); }
  .breadcrumb__separator { color: var(--color-text-on-dark); }
  .article-header__category {
    display: inline-block;
    font-size: var(--text-xs);
    font-weight: var(--font-semibold);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-primary);
    background: var(--color-primary-bg);
    padding: 0.375rem 0.875rem;
    border-radius: 2rem;
    margin-bottom: 1rem;
  }
  .article-header__title {
    font-family: var(--font-serif);
    font-size: var(--text-section-title);
    font-weight: var(--font-bold);
    line-height: 1.2;
    margin-bottom: 1rem;
  }
  .article-header__subtitle {
    font-size: var(--text-xl);
    color: var(--color-text-secondary);
    line-height: var(--leading-snug);
    margin-bottom: 1.5rem;
  }
  .article-header__meta {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    flex-wrap: wrap;
    font-size: 0.9375rem;
    color: var(--color-text-muted);
  }
  .article-header__author {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }
  .article-header__author-image {
    width: 2.5rem;
    height: 2.5rem;
    border-radius: var(--radius-full);
    object-fit: cover;
  }
  .article-header__author-name {
    font-weight: var(--font-semibold);
    color: var(--color-text);
  }
  .article-header__date,
  .article-header__reading-time {
    display: flex;
    align-items: center;
    gap: 0.375rem;
  }
</style>
